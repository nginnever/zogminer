Quick README to compile. 

The kernel is written as a byte array and stored into a header file. This is automated by cmake. 

To build, just do the usual:

mkdir build && cd build && cmake .. && make


